#include <stdio.h>

/**
 * main - Prints numbers less than 10, starting from 0
 *
 * Return: Always 0
 */
int main(void)
{
	int i = 0;
	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}
	return (0);
}
