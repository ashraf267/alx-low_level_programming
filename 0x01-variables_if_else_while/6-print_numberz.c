#include <stdio.h>
/* More headers can be added here */

/**
 * main - Same as before, just different rules
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 48;
	while (i <= 57)
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
