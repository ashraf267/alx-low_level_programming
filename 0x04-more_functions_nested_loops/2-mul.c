#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: parameter one
 * @b: parameter two
 *
 * Return: Returns the computed product
 */
int mul(int a, int b)
{
	return (a * b);
}
