#include "main.h"

/**
 * _strspn - gets the len of a prefix substr
 * @s: given str
 * @accept: given prefix substr
 *
 * Return: number of bytes in the init segment of s
 */

unsigned int _strspn(char *s, char *accept)
{
	/* emp func dev */
}
