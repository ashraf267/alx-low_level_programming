#include "main.h"
#include <ctype.h>

/**
 * _isupper: checks for uppercase char
 * @c: the char to be checked
 *
 * Return: if uppercase, return 1
 * else, return 0
 */

int _isupper(int c)
{
	/* empty function def */
}
